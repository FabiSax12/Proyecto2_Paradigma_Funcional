<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Proyecto2_Lenguajes.GUI.ViewModels"
        x:Class="Proyecto2_Lenguajes.GUI.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Proyecto F# - Selector de Juegos"
        Width="500" Height="460"
        WindowStartupLocation="CenterScreen">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <StackPanel Margin="30" Spacing="30" VerticalAlignment="Center">

        <!-- Título -->
        <TextBlock Text="🎮 Juegos en F#"
                  FontSize="28"
                  FontWeight="Bold"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,20"/>

        <TextBlock Text="Selecciona un juego para comenzar:"
                  FontSize="16"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,20"/>

        <!-- Botón Ahorcado -->
        <Button Content="🎯 AHORCADO"
                Command="{Binding AhorcadoCommand}"
                FontSize="20"
                FontWeight="Bold"
                Padding="20"
                Width="250"
                Height="60"
                HorizontalAlignment="Center"
                Background="LightCoral"
                CornerRadius="10"/>

        <!-- Botón Sopa de Letras -->
        <Button Content="🔤 SOPA DE LETRAS"
                Command="{Binding SopaLetrasCommand}"
                FontSize="20"
                FontWeight="Bold"
                Padding="20"
                Width="250"
                Height="60"
                HorizontalAlignment="Center"
                Background="LightGreen"
                CornerRadius="10"/>

        <!-- Botón Sopa de Letras -->
        <Button Content="SALIR"
                Command="{Binding SalirCommand}"
                FontSize="20"
                FontWeight="Bold"
                Padding="20"
                Width="250"
                Height="60"
                HorizontalAlignment="Center"
                Background="Red"
                CornerRadius="10"/>

    </StackPanel>
</Window>